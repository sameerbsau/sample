package streams;

public class SQLQueries {
//    SELECT O.CustomerID, SUM(subq.ordertotal) as total FROM Orders O Left join
//    (SELECT o.OrderID, SUM(o.Quantity*p.Price) as ordertotal FROM OrderDetails o
//    LEFT JOIN Products p on o.ProductId = p.ProductId group by o.OrderID) subq on O.OrderID = subq.OrderID
//    group by O.CustomerID  ;


}